type = "container"

build = """
    FROM node
    WORKDIR /app
    COPY . .
"""
run-writable-file-system = true

run-cmd = ["docker-entrypoint.sh","node", "index-1.js"]

[expose.3000]
type = "http-domain"