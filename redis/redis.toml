type = "pod"

build = "FROM redis"
run-cmd = ["docker-entrypoint.sh", "redis-server"]
run-writable-file-system=true

[expose.6379]
type = "tcp"

[storage."/tmp"]
type = "block"
max-size-mb = 100

[storage."/data"]
type = "block"
max-size-mb = 100